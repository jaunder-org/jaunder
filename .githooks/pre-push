#!/usr/bin/env bash
set -euo pipefail

if [[ "${SKIP_PRE_PUSH:-0}" == "1" ]]; then
    echo "--- pre-push: SKIP_PRE_PUSH=1, skipping ---"
    exit 0
fi

echo "--- pre-push: running cargo xtask prepush ---"
cargo xtask prepush

echo "--- pre-push: all checks passed ---"
