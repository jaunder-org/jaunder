[profile.default]
# Warn about slow tests after 5 seconds; kill them after 30 seconds.
# Individual tests should complete in well under a second.
slow-timeout = { period = "5s", terminate-after = 6 }

[profile.coverage]
junit = { path = "/tmp/jaunder-coverage-junit.xml", report-skipped = "ignored" }

[profile.coverage-worker-1]
junit = { path = "/tmp/jaunder-coverage-worker-1-junit.xml", report-skipped = "ignored" }

[profile.coverage-worker-2]
junit = { path = "/tmp/jaunder-coverage-worker-2-junit.xml", report-skipped = "ignored" }
