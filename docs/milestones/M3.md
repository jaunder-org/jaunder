# Milestone 3: Email and SMTP

## Scope

M3 adds SMTP relay configuration, an injectable mailer abstraction, optional email addresses on
user accounts with a verification flow, and password reset via email.

Jaunder does not perform direct mail delivery or MX lookups. It hands outbound messages to a
configured relay (e.g. Postfix, SendGrid, Mailgun's SMTP endpoint) which is responsible for final
delivery.

### SMTP configuration

SMTP settings are stored as site_config keys:

| Key | Type | Description |
|---|---|---|
| `smtp.host` | `TEXT` | Relay hostname |
| `smtp.port` | `INTEGER` | Port number |
| `smtp.tls_mode` | `TEXT` | `"plain"`, `"starttls"`, or `"tls"` |
| `smtp.username` | `TEXT` | Optional credentials username |
| `smtp.password` | `TEXT` | Optional credentials password |
| `smtp.sender` | `TEXT` | Sender address (e.g. `"Jaunder <noreply@example.com>"`) |

`SmtpTlsMode` enum (`Plain`, `StartTls`, `Tls`) with `FromStr`/`Display` lives in `common`.
`SmtpConfig { host, port, tls_mode, username, password, sender }` and
`load_smtp_config(store: &dyn SiteConfigStorage) -> Option<SmtpConfig>` (returns `None` when
`smtp.host` is absent) also live in `common`.

### `EmailMessage` and `MailSender`

`EmailMessage` (in `common`) is a transport-neutral representation of an outbound message:

```rust
pub struct EmailMessage {
    pub from: Option<email_address::EmailAddress>,   // overrides SmtpConfig sender when Some
    pub to: Vec<email_address::EmailAddress>,
    pub subject: String,
    pub body_text: String,
}
```

Multiple recipients are supported via `to: Vec<email_address::EmailAddress>`. Per-message sender override via `from`.
When HTML support is needed, add `body_html: Option<String>` — `LettreMailSender` builds a
`MultiPart::alternative()` when it is `Some`, without changing the `send_email` signature.

`MailSender` (in `common`) sends an `EmailMessage`:

```rust
#[async_trait]
pub trait MailSender: Send + Sync {
    async fn send_email(&self, message: &EmailMessage) -> Result<(), MailError>;
}
```

`MailError` (in `common`): `NotConfigured` (SMTP not set up), `Send(String)` (transport error).

Three implementations:

- **`NoopMailSender`** (in `common`) — returns `MailError::NotConfigured`. Used when SMTP config is
  absent at startup.
- **`LettreMailSender`** (in `server`) — builds a `lettre` transport from `SmtpConfig` at startup,
  converts `EmailMessage` to a `lettre::Message`, and sends it.
- **`CapturingMailSender`** (in `common`, behind a `test-utils` feature) — stores sent messages in
  a `Mutex<Vec<EmailMessage>>` for assertions in integration tests across crates.

`AppState` gains `mailer: Arc<dyn MailSender>`. `open_database` selects `NoopMailSender` or
`LettreMailSender` based on whether `load_smtp_config` returns `Some`.

### `jaunder smtp test` command

`jaunder smtp test --to <email>` loads SMTP config from the database, returns an error if SMTP is
not configured, otherwise sends a test message to the given address and reports success or failure.

### Email on user accounts

The `users` table gains two columns:

```sql
email          TEXT,
email_verified INTEGER NOT NULL DEFAULT 0
```

`UserRecord` gains `email: Option<String>` and `email_verified: bool`. `UserStorage` gains
`set_email(user_id: i64, email: Option<&str>, verified: bool) -> sqlx::Result<()>`.

A new table stores pending verification tokens (hash stored, raw token delivered by email):

```sql
email_verifications(
    token_hash  TEXT PRIMARY KEY,
    user_id     INTEGER NOT NULL REFERENCES users(user_id),
    email       TEXT NOT NULL,
    created_at  TEXT NOT NULL,
    expires_at  TEXT NOT NULL,
    used_at     TEXT
)
```

Timestamps are RFC 3339 text in SQLite, `chrono::DateTime<Utc>` in Rust (consistent with M2).

`EmailVerificationStorage` trait (in `common`):
- `create_email_verification(user_id: i64, email: &str, expires_at: DateTime<Utc>) -> sqlx::Result<String>` — stores hash, returns raw token.
- `use_email_verification(raw_token: &str) -> Result<(i64, String), UseEmailVerificationError>` — validates, marks used, returns `(user_id, email)`.

`UseEmailVerificationError`: `NotFound`, `Expired`, `AlreadyUsed`.

A user can have at most one pending verification at a time. Creating a new verification supersedes
any prior unverified token (the old token becomes `Expired` or `NotFound` on next use; no explicit
deletion is required).

### Password reset

A new table stores password reset tokens:

```sql
password_resets(
    token_hash  TEXT PRIMARY KEY,
    user_id     INTEGER NOT NULL REFERENCES users(user_id),
    created_at  TEXT NOT NULL,
    expires_at  TEXT NOT NULL,
    used_at     TEXT
)
```

`PasswordResetStorage` trait (in `common`):
- `create_password_reset(user_id: i64, expires_at: DateTime<Utc>) -> sqlx::Result<String>`
- `use_password_reset(raw_token: &str) -> Result<i64, UsePasswordResetError>` — returns `user_id`.

`UsePasswordResetError`: `NotFound`, `Expired`, `AlreadyUsed`.

`UserStorage` gains `set_password(user_id: i64, new_password: &str) -> sqlx::Result<()>` (hashes
internally via `spawn_blocking`, consistent with `create_user`).

Reset flow:
1. User submits username on `/forgot-password`.
2. Server looks up user by username. If no verified email is on file (user not found, or found but
   no verified email), returns an error telling the user to contact the site operator. Otherwise
   creates a token, sends a link to the verified email, and returns success. The success response
   does not confirm whether the username exists (avoids enumeration).
3. The link goes to `/reset-password?token=...`. User enters a new password.
4. `confirm_password_reset` validates the token, sets the new password, revokes all existing
   sessions for that user, and redirects to `/login`.

### Web UI

Four new routes:

| Path | Component | Auth required |
|---|---|---|
| `/profile/email` | `EmailPage` | Yes |
| `/verify-email` | `VerifyEmailPage` | No |
| `/forgot-password` | `ForgotPasswordPage` | No |
| `/reset-password` | `ResetPasswordPage` | No |

`EmailPage`: shows current email and verification status. Form to submit a new email address, which
calls `request_email_verification` — creates a token and sends the verification link. On submit the
page confirms "check your email."

`VerifyEmailPage`: reads `token` from the query string, calls `use_email_verification`, then
`set_email(..., verified: true)`, and renders success or an appropriate error (expired, already
used, not found).

`ForgotPasswordPage`: username form. On submit calls `request_password_reset` (see reset flow
above).

`ResetPasswordPage`: reads `token` from the query string, shows a new-password form. On submit
calls `confirm_password_reset`.

## Steps

After each step verify:

1. `cargo build` succeeds.
2. `cargo nextest run` passes.
3. `cargo clippy -- -D warnings` is clean.
4. `nix flake check` passes.

Wait for user review, commit when the user says that the review is complete and successful.

### Step 1: Cargo dependencies

1. [x] Add `lettre = { version = "0.11", default-features = false, features = ["tokio1", "tokio1-rustls-tls", "smtp-transport", "builder"] }` to
   `[workspace.dependencies]` in the root `Cargo.toml`.
2. [x] Add `lettre` to `[dependencies]` in `server/Cargo.toml`.
3. [x] Verify `cargo build` succeeds with the new dependencies.

### Step 2: SMTP configuration types

1. [x] Define `SmtpTlsMode` enum (`Plain`, `StartTls`, `Tls`) in `common/src/smtp.rs` with
   `FromStr` (`"plain"`, `"starttls"`, `"tls"`) and `Display`; declare as `pub mod smtp;` in
   `common/src/lib.rs`.
2. [x] Define `SmtpConfig { host: String, port: u16, tls_mode: SmtpTlsMode, username: Option<String>, password: Option<String>, sender: email_address::EmailAddress }` in `common/src/smtp.rs`.
3. [x] Implement `pub fn load_smtp_config(store: &dyn SiteConfigStorage) -> Option<SmtpConfig>` in
   `common/src/smtp.rs`; returns `None` when `smtp.host` is absent.
4. [x] Unit test: `SmtpTlsMode` parses each variant; unknown string returns error.
5. [x] Unit test: `load_smtp_config` returns `None` when `smtp.host` absent; returns `Some` with
   correct fields when all keys present.

### Step 3: `EmailMessage`

1. [x] Define `EmailMessage { from: Option<email_address::EmailAddress>, to: Vec<email_address::EmailAddress>, subject: String, body_text: String }`
   in `common/src/mailer.rs`; declare as `pub mod mailer;` in `common/src/lib.rs`.
2. [x] Unit test: `EmailMessage` fields are accessible; `from` defaults to `None`; `to` supports
   multiple recipients.

### Step 4: `MailSender` trait and implementations

1. [x] Define `MailError` (variants `NotConfigured`, `Send(String)`) in `common/src/mailer.rs`
   using `thiserror`.
2. [x] Define `#[async_trait] pub trait MailSender: Send + Sync` with
   `send_email(&self, message: &EmailMessage) -> Result<(), MailError>` in `common/src/mailer.rs`.
3. [x] Implement `NoopMailSender` in `common/src/mailer.rs` returning `MailError::NotConfigured`.
4. [x] Implement `CapturingMailSender { sent: Mutex<Vec<EmailMessage>> }` in `common/src/mailer.rs`
   behind `#[cfg(any(test, feature = "test-utils"))]`. Expose `fn sent(&self) -> Vec<EmailMessage>`
   for assertions.
5. [x] Add `test-utils` feature to `common/Cargo.toml`.
6. [x] Add `common = { ..., features = ["test-utils"] }` to `[dev-dependencies]` in
   `server/Cargo.toml` and `web/Cargo.toml`.
7. [x] Implement `LettreMailSender { mailer: AsyncSmtpTransport<Tokio1Executor>, sender: Mailbox }`
   in `server/src/mailer.rs`; declare as `pub mod mailer;` in `server/src/lib.rs`. Constructor
   builds the transport from `SmtpConfig` selecting the appropriate lettre transport variant per
   `SmtpTlsMode`. Uses `message.from` when `Some`, otherwise falls back to `self.sender`.
8. [x] Unit test: `NoopMailSender::send_email` returns `MailError::NotConfigured`.
9. [x] Unit test: `CapturingMailSender::send_email` stores the message; `sent()` returns it.

### Step 5: `AppState` expansion

1. [x] Add `email_verifications: Arc<dyn EmailVerificationStorage>` to `AppState` in
   `common/src/storage.rs` (stub the trait as an empty `trait EmailVerificationStorage: Send + Sync {}` for now).
2. [x] Add `password_resets: Arc<dyn PasswordResetStorage>` to `AppState` similarly (stub).
3. [x] Add `mailer: Arc<dyn MailSender>` to `AppState`.
4. [x] Update `open_database` to construct stubs and select `NoopMailSender` or `LettreMailSender`
   based on `load_smtp_config`.
5. [x] Update all existing tests broken by the `AppState` change.

### Step 6: Email column on users

1. [x] Create `server/migrations/0005_add_email_to_users.sql` adding `email TEXT` and
   `email_verified INTEGER NOT NULL DEFAULT 0` to `users`.
2. [x] Add `email: Option<email_address::EmailAddress>` and `email_verified: bool` to `UserRecord` in
   `common/src/storage.rs`.
3. [x] Add `set_email(user_id: i64, email: Option<&email_address::EmailAddress>, verified: bool) -> sqlx::Result<()>` to
   `UserStorage` trait; implement in `SqliteUserStorage`.
4. [x] Update all `UserRecord` construction sites to include the new fields.
5. [x] Integration test: `set_email` persists values; a subsequent `get_user` reflects them.
6. [x] Integration test: `set_email(user_id, None, false)` clears a previously set email.

### Step 7: `EmailVerificationStorage`

1. [x] Create `server/migrations/0006_create_email_verifications.sql` with the
   `email_verifications` schema.
2. [x] Define `UseEmailVerificationError` (`NotFound`, `Expired`, `AlreadyUsed`) in
   `common/src/storage.rs`.
3. [x] Replace the stub `EmailVerificationStorage` with the full trait in `common/src/storage.rs`:
   `create_email_verification`, `use_email_verification`.
4. [x] Implement `SqliteEmailVerificationStorage { pool: SqlitePool }` in
   `server/src/storage/sqlite.rs` (or its own module). Token generation delegates to
   `generate_token()`; storage follows the same hash pattern as sessions and invites.
5. [x] Update `open_database` to construct `SqliteEmailVerificationStorage` and wire it into
   `AppState`.
6. [x] Integration test: `create_email_verification` returns a token; `use_email_verification` with
   that token returns the correct `(user_id, email)` and marks it used.
7. [x] Integration test: `use_email_verification` on an already-used token returns `AlreadyUsed`.
8. [x] Integration test: `use_email_verification` on an expired token returns `Expired`.
9. [x] Integration test: `use_email_verification` on an unknown token returns `NotFound`.
10. [x] Integration test: creating a second verification for the same user supersedes the first
    (first token returns `NotFound` or `Expired` after the second is created and used).

### Step 8: `PasswordResetStorage`

1. [x] Create `server/migrations/0007_create_password_resets.sql` with the `password_resets`
   schema.
2. [x] Add `set_password(user_id: i64, new_password: &str) -> sqlx::Result<()>` to `UserStorage`;
   implement in `SqliteUserStorage` using `spawn_blocking` for hashing.
3. [x] Define `UsePasswordResetError` (`NotFound`, `Expired`, `AlreadyUsed`) in
   `common/src/storage.rs`.
4. [x] Replace the stub `PasswordResetStorage` with the full trait in `common/src/storage.rs`:
   `create_password_reset`, `use_password_reset`.
5. [x] Implement `SqlitePasswordResetStorage { pool: SqlitePool }`.
6. [x] Update `open_database` to construct `SqlitePasswordResetStorage` and wire it into
   `AppState`.
7. [x] Integration test: `set_password` changes the password; subsequent `authenticate` with the
   old password returns `InvalidCredentials` and with the new password returns the `UserRecord`.
8. [x] Integration test: `create_password_reset` returns a token; `use_password_reset` with that
   token returns the correct `user_id` and marks it used.
9. [x] Integration test: `use_password_reset` on an already-used token returns `AlreadyUsed`.
10. [x] Integration test: `use_password_reset` on an expired token returns `Expired`.
11. [x] Integration test: `use_password_reset` on an unknown token returns `NotFound`.

### Step 9: `jaunder smtp test` command

1. [ ] Add `SmtpTest { storage: StorageArgs, to: String }` to `Commands` in `cli.rs`.
2. [ ] Dispatch in `main.rs`.
3. [ ] Implement `cmd_smtp_test`: open existing database; call `load_smtp_config`; return an error
   if not configured; construct `LettreMailSender`; send an `EmailMessage` to `--to`; print success
   or error.
4. [ ] CLI unit test: `smtp-test` parses `--to`.
5. [ ] CLI unit test: missing `--to` is a clap error.

### Step 10: Web UI — email verification flow

1. [ ] Create `web/src/email.rs`; declare as `pub mod email;` in `web/src/lib.rs`.
2. [ ] Implement `request_email_verification(email: String) -> Result<(), ServerFnError>` server
   function: requires auth; calls `create_email_verification`; constructs and sends an
   `EmailMessage` with the verification link (`/verify-email?token=...`); returns `Ok`.
3. [ ] Implement `verify_email(token: String) -> Result<(), ServerFnError>` server function: calls
   `use_email_verification`; on success calls `set_email(..., verified: true)`; maps
   `UseEmailVerificationError` variants to descriptive `ServerFnError` messages.
4. [ ] Define `EmailPage` component: shows current email and verified status from `get_profile`;
   form to submit a new address calling `request_email_verification`; displays "check your email"
   on success.
5. [ ] Define `VerifyEmailPage` component: reads `token` query parameter; calls `verify_email`;
   renders success or error.
6. [ ] Add `/profile/email` and `/verify-email` routes to `App` in `web/src/lib.rs`.
7. [ ] Integration test: `request_email_verification` creates a verification row and calls
   `send_email` on the `CapturingMailSender` with the correct recipient and a body containing the
   token.
8. [ ] Integration test: `verify_email` with a valid token sets the email as verified on the user.
9. [ ] Integration test: `verify_email` with an expired token returns an error.
10. [ ] Integration test: `verify_email` with an unknown token returns an error.
11. [ ] E2E test: user adds an email address on `/profile/email`; extract the verification token
    from the sent email; visit `/verify-email?token=...`; confirm the email is shown as verified
    on `/profile/email`.
12. [ ] E2E test: visiting `/verify-email` with an invalid token shows an error.

### Step 11: Web UI — password reset flow

1. [ ] Create `web/src/password_reset.rs`; declare as `pub mod password_reset;` in
   `web/src/lib.rs`.
2. [ ] Implement `request_password_reset(username: String) -> Result<(), ServerFnError>` server
   function: looks up user by username; if user not found or has no verified email, returns an
   error telling the user to contact the site operator; otherwise creates a reset token, sends an
   `EmailMessage` with the reset link (`/reset-password?token=...`), and returns success. The
   success response does not confirm whether the username existed.
3. [ ] Implement `confirm_password_reset(token: String, new_password: String) -> Result<(), ServerFnError>`
   server function: calls `use_password_reset`; sets new password via `set_password`; revokes all
   sessions for that user via `list_sessions` + `revoke_session`; returns `Ok`.
4. [ ] Define `ForgotPasswordPage` component: username form calling `request_password_reset`;
   on success renders a neutral confirmation message (does not confirm whether the username
   exists); on error (no verified email / contact operator) surfaces the error message directly.
5. [ ] Define `ResetPasswordPage` component: reads `token` query parameter; new-password form
   calling `confirm_password_reset`; on success redirects to `/login`.
6. [ ] Add `/forgot-password` and `/reset-password` routes to `App` in `web/src/lib.rs`.
7. [ ] Integration test: `request_password_reset` for a user with a verified email sends a reset
   email via `CapturingMailSender` with a body containing the token.
8. [ ] Integration test: `request_password_reset` for a user without a verified email returns an
   error.
9. [ ] Integration test: `request_password_reset` for an unknown username returns an error.
10. [ ] Integration test: `confirm_password_reset` with a valid token sets the new password and
    revokes all existing sessions.
11. [ ] Integration test: `confirm_password_reset` with an expired token returns an error.
12. [ ] Integration test: `confirm_password_reset` with an already-used token returns an error.
13. [ ] E2E test: user requests a password reset on `/forgot-password`; extract the reset token
    from the sent email; visit `/reset-password?token=...`; submit a new password; confirm login
    with the new password succeeds and login with the old password fails.
14. [ ] E2E test: visiting `/reset-password` with an invalid token shows an error.
15. [ ] E2E test: submitting `/forgot-password` for a user with no verified email shows the
    "contact operator" error message.
