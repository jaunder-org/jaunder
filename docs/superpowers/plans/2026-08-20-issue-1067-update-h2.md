# H2 security update Implementation Plan

> **For agentic workers:** Execute this plan task-by-task with `jaunder-iterate`
> (delegating individual tasks to a subagent via `jaunder-dispatch` when
> useful). Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the vulnerable locked `h2` release with the minimal
cargo-deny-accepted patch update.

**Architecture:** Cargo's existing transitive constraint permits the safe patch
release. Update the one lockfile package, inspect the resulting lock delta, then
certify the host gate; no manifest or application code changes are needed.

**Tech Stack:** Cargo lockfile; Cargo package resolver; cargo-deny via xtask.

## Review header

**Scope — in:** `Cargo.lock`'s `h2` package entry and its checksum.

**Scope — out:** every `Cargo.toml`, application source, cargo-deny
configuration, and advisory-ignore policy.

**Tasks:**

1. Resolve and verify the minimal safe `h2` lockfile update.

**Key decisions/risks:** The dry run selected 0.4.17 from 0.4.14. If resolution
changes more than `h2`, stop rather than accepting unrelated lockfile churn.

**Specification:**
[`2026-08-20-issue-1067-update-h2.md`](../specs/2026-08-20-issue-1067-update-h2.md).

---

## Global Constraints

- Do not edit `Cargo.toml`, application code, cargo-deny configuration, or an
  advisory-ignore list.
- Accept only one lockfile package update: `h2` from 0.4.14 to 0.4.17 or a later
  cargo-deny-accepted safe release.
- The host acceptance command is `devtool run -- cargo xtask check --no-test`.
- Before committing, tick this task's checkboxes, run
  `devtool run -- cargo xtask check`, stage the checked tree, and commit without
  a `Co-Authored-By` trailer.

### Task 1: Update the locked H2 package

**Files:**

- Modify: `Cargo.lock:h2` package entry
- Create: `docs/superpowers/specs/2026-08-20-issue-1067-update-h2.md`
- Create: `docs/superpowers/plans/2026-08-20-issue-1067-update-h2.md`

**Interfaces:**

- Consumes: Cargo's current lockfile resolution and the existing transitive `h2`
  version constraint from `hyper`.
- Produces: one `Cargo.lock` package entry with `name = "h2"`, a safe version,
  and the registry checksum matching that release.

- [x] **Step 1: Resolve the patch update**

  Run:

  ```bash
  devtool run -- cargo update -p h2
  ```

  Inspect `Cargo.lock` and its diff. Require exactly one changed package entry:
  `h2` moves from 0.4.14 to 0.4.17 or later and receives its matching checksum.
  If Cargo re-resolves unrelated existing dependency edges, restore that churn;
  then require `cargo xtask check --no-test` to preserve the one-entry update
  before proceeding.

- [x] **Step 2: Prove the advisory gate is clear**

  Run:

  ```bash
  devtool run -- cargo xtask check --no-test
  ```

  Expected: PASS, including cargo-deny; no RUSTSEC-2026-0258 advisory failure.

- [x] **Step 3: Commit the verified lockfile update**

  Tick Task 1, run `devtool run -- cargo xtask check`, inspect and stage
  `Cargo.lock`, the specification, and this plan, then commit:

  ```bash
  git commit -m "chore(deps): update h2 security fix"
  ```

## Self-review

- **Spec coverage:** Task 1 updates the safe resolution (AC1), confines the diff
  to `h2`'s lockfile entry (AC2), and runs the specified cargo-deny gate (AC3).
- **Placeholder scan:** no unspecified implementation or verification work
  remains.
- **Type consistency:** not applicable; this change introduces no code
  interface.

## Execution handoff

Plan complete and saved to
`/home/mdorman/src/jaunder/agent-7/docs/superpowers/plans/2026-08-20-issue-1067-update-h2.md`.

After approval, execute it with `jaunder-iterate`, keeping the lockfile diff
minimal and using `jaunder-commit` before the commit.
