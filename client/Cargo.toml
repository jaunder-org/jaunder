[package]
name = "client"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-only"

[dependencies]
wasm-bindgen = { workspace = true }
web-sys = { workspace = true, features = ["Window", "Storage"] }

[lints]
workspace = true
